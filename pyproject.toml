[project]
name = "cse-resnet-ref"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "koozie>=1.3.0",
    "resdx",
]

[tool.uv.sources]
resdx = { git = "https://github.com/bigladder/resdx.git", rev = "main" }
